version: 2

updates:
  - directory: '/'
    package-ecosystem: 'github-actions'
    schedule:
      interval: 'weekly'
    groups:
      dependencies:
        patterns:
          - '*'
    cooldown:
      default-days: 14
